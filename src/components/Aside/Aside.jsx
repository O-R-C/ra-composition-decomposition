import Grammar from '../Grammar/Grammar'

/**
 * A React functional component that renders an aside element.
 *
 * @return {JSX.Element} The rendered aside element.
 */
const Aside = () => {
  return (
    <div className='aside'>
      <Grammar />
    </div>
  )
}

export default Aside
